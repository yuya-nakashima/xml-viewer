# XMLビューア

zip解凍後のフォルダから XML / XSL を読み込み、帳票を表示するビューアです。

## 使い方

1. zipを解凍します
2. XMLファイルとXSLファイルが直接入っているフォルダを選択します
3. タブを切り替えて表示します

## 補足

- 選択したフォルダ直下のファイルだけを対象にします
- 直下にXMLファイルがない場合は、さらに内側のフォルダを選択してください
- `a.xml` と `a.xsl` のような同名ファイルを自動で組み合わせます